package org.example.GUI.gamestates;

/**
 * The Couleur enum represents different colors that can be used in the game.
 * This enum includes predefined colors: ROUGE (red), BLEU (blue), VERT (green), JAUNE (yellow).
 */
public enum Couleur {
    ROUGE, BLEU, VERT, JAUNE;
}
